/**
 * Declarações da linguagem (this)
 * Parametros formais
 * Declaração de functions (métodos)
 * Declaração de variaveis
 **/

debug(getX());

var getX = function () {
  return 'x';
}

function getX() {
  return 'xx';
}